#ifndef RESOURCE_PATH_HPP
#define RESOURCE_PATH_HPP

#include <string>


// The path to the resource folder associate with the main bundle or
// an empty string is there is no bundle.
std::string resourcePath(void);

#endif
